# Phase-0 kernel snapshot

This directory is the **live Phase-0 engine** copied from the running city, not a full app scaffold.

- `engine.ts` — join, act, perception, seed
- `constitution.ts` — 3.1
- `seed.ts` — Arrival furnishings, settlers as history
- `awf-phase1.mjs` — 17 invariants
- SQL — places, residents, things, notes, pacts, events, memories, portals

It runs inside a TanStack Start app. Treat this as reference for phases 10+, not as “clone and you have production hosting.”
